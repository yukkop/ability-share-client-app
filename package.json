{
  "name": "ability-share-client-app",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "yukkop, shuff",
  "license": "ISC",
  "devDependencies": {
    "@types/electron": "^1.6.10",
    "electron": "^20.0.2"
  },
  "dependencies": {
    "rxjs": "^7.5.6",
    "typescript": "^4.7.4"
  }
}
